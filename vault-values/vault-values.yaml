server:
  dev:
    enabled: true
    devRootToken: "root"
  logLevel: debug
  
  ingress:
    enabled: true
    annotations:
      kubernetes.io/ingress.class: nginx
      nginx.ingress.kubernetes.io/rewrite-target: /

injector:
  enabled: "false"